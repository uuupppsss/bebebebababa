<Window x:Class="DataTemplateAppSimple.Editors.GPUAdd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataTemplateAppSimple"
        mc:Ignorable="d"
        Title="GPUAdd" Height="450" Width="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FFED882F"/>
                <GradientStop Color="#FF701717" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <StackPanel>
            <Label Content="Name"/>
            <TextBox Text="{Binding NewGPU.Title}"/>
            <Label Content="Volume"/>
            <TextBox Text="{Binding NewGPU.Capasity}"/>
            <Label Content="Frequency"/>
            <TextBox Text="{Binding NewGPU.Frequency}"/>
            <Label Content="Prise"/>
            <TextBox Text="{Binding NewGPU.Price}"/>
        </StackPanel>
    </Grid>
</Window>
